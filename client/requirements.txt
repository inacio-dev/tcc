# ============================================================================
# CLIENT REQUIREMENTS - F1 Remote Car Project
# ============================================================================
# Instale com: pip install -r requirements.txt
#
# PRE-REQUISITOS (Linux):
#   sudo apt-get install python3-tk
#
# Arch Linux:
#   sudo pacman -S tk
# ============================================================================

# OpenCV para processamento de video e exibicao
opencv-python>=4.8.0

# NumPy para operacoes com arrays
numpy>=1.24.0

# Pillow para processamento de imagem (Tkinter)
Pillow>=10.0.0

# PySerial para comunicacao serial com ESP32 (simulador)
pyserial>=3.5

# NOTA: PyAV (av) foi removido pois não usamos mais H.264
# O sistema utiliza apenas MJPEG para transmissão de vídeo
# Ver: docs/CAMERA_VIDEO_ISSUES.md para detalhes da decisão

# Matplotlib para graficos de telemetria F1 em tempo real
matplotlib>=3.7.0

# CuPy para processamento de imagem na GPU NVIDIA (OPCIONAL)
# Requer CUDA Toolkit instalado no sistema!
# Arch Linux: sudo pacman -S cuda
# Ubuntu: sudo apt install nvidia-cuda-toolkit
#
# Depois instale manualmente:
#   CUDA 12.x: pip install cupy-cuda12x
#   CUDA 11.x: pip install cupy-cuda11x
#
# Se nao tiver GPU/CUDA, o sistema usa CPU automaticamente
